import type {PathRef} from '../../utils/file-ref.js';

export class AwspParser {
  async parseAWSP(_path: PathRef): Promise<Record<string, unknown>> {
    // Placeholder parser logic - return empty result for now
    return {};
  }
}
